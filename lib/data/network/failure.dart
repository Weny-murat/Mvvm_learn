//Failure(beklenenin olmaması) sınıfı işlerin bir şekilde ters gittiğini kodlamaya yansıtan sınıfımız
class Failure {
  int code;
  String message;

  Failure(
    this.code,
    this.message,
  );
}
